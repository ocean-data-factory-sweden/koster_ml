version: '3'
services:
  fastapi:
    build: fastapi/
    ports:
      - '5000:5000'
    volumes:
      - './model_config:/data'
      - './uploads:/uploads'
    networks:
      - deploy_network
    container_name: fastapi
  streamlit:
    build: streamlit/
    depends_on:
      - fastapi
    ports:
      - '8501:8501'
    volumes:
      - './model_config:/data'
      - './uploads:/uploads'
    networks:
      - deploy_network
    container_name: streamlit
networks:
  deploy_network:
    driver: bridge
